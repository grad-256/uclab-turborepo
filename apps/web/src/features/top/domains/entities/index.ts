export function isZero(value: number): boolean {
  return value === 0
}

export function isZerov2(value: number): boolean {
  return value === 0
}

export function sub(a: number, b: number) {
  return a - b
}
